# Copy to ops/backup.env; chmod 600. Never commit the filled file.
# Install age and rsync on the VPS. Store the private age key OFF the VPS.
AGE_RECIPIENT=age1replace
BACKUP_DEST=backup-user@independent-host:/srv/autohistory/
DELETION_JOURNAL_HOST_DIR=/mnt/independent-storage/autohistory-deletions
